# NeoForge
neo_version=26.1.0.11-beta
minecraft_version=26.1
minecraft_version_range=[26.1,27)
neo_version_range=[26.1,)
loader_version_range=[2,)

# Mod
mod_id=miq
mod_name=Most Important Question
mod_license=GPLv3
mod_version=1.1.0-Neoforge-26.1
mod_group_id=com.cloversecho.miq
mod_authors=Clovers_Echo
mod_description=What's to eat today?

org.gradle.jvmargs=-Xmx4G -XX:+UseZGC
org.gradle.daemon=true
# The JDK 25 used for the Java 25 toolchain. Auto-detection flags the registered
# install as invalid and the foojay auto-download target is blocked in this network,
# so point Gradle at the toolchain explicitly and disable both detections.
org.gradle.java.installations.paths=C:/Program Files/Java/jdk-25.0.4
org.gradle.java.installations.auto-detect=true
org.gradle.java.installations.auto-download=false