# Forge
forge_version=47.2.0
minecraft_version=1.20.1
minecraft_version_range=[1.20.1,1.21)
forge_version_range=[47,)
loader_version_range=[47,)

# Mod
mod_id=miq
mod_name=Most Important Question
mod_license=GPLv3
mod_version=1.1.0-Forge-1.20.1
mod_group_id=com.cloversecho.miq
mod_authors=Clovers_Echo
mod_description=What's to eat today?

org.gradle.jvmargs=-Xmx4G -XX:+UseZGC
org.gradle.daemon=true

# Run the Gradle daemon on JDK 17 so it matches the project toolchain.
# (User-level gradle.properties points org.gradle.java.home at JDK 21, which
#  breaks ForgeGradle's MCP toolchain lookup with "DefaultToolchainSpec" errors.)
org.gradle.java.home=C:\\Users\\ASUS\\.jdks\\jdk-17.0.2
